---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${ pvc-claim }
  namespace: ${ namespace }
spec:
  storageClassName: ${ storage-name }
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
